<%-#a partial form for :notes which contains the variables :title and :text-%>
<%-#form_for is a helper method, notes_path points the form towards the "notes" prefix and sends a POST request this calls the Create action%>
<%= form_for @note do |f| %>
    <%-# @note.errors.any? checks if there are any errors %>
    <% if @note.errors.any? %>
        <div id = "error_explanation">
          <h2>
            <%-# pluralize is a helper that takes a number and string as its arguments %>
            <%= pluralize(@note.errors.count, "error") %> prohibited
            this article from being saved:
          </h2>
          <ul>
            <% @note.errors.full_messages.each do |msg| %>
                <li> <%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <p>
      <%= f.label :title %><br>
      <%= f.text_field :title %>
    </p>

    <p>
      <%= f.label :text %><br>
      <%= f.text_area :text %>
    </p>

    <p>
      <%= f.submit %>
    </p>

<% end %>